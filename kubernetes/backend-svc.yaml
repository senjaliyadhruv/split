apiVersion: v1
kind: Service
metadata:
  name: splitwise-backend
  labels:
    app: splitwise
    tier: backend
spec:
  type: NodePort
  selector:
    app: splitwise
    tier: backend
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30000
      protocol: TCP
      name: http
